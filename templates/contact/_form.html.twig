<section class="contact-page-form__flux">
{{ form_start(form, {'attr': {'class': 'contact-page-form__form'}}) }}
    {{ form_widget(form) }}
    <div class='flex-center contact-page-form__modification'>
        <button class="btn btn__linear-gradient">{{ button_label|default('Sauvegarder') }}</button>
    </div>
{{ form_end(form) }}
</section>
